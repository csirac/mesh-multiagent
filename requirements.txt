# Mesh — Python dependencies
#
# Core (required for router, agents, and TUI):
aiohttp>=3.9
httpx>=0.27
openai>=1.0
PyYAML>=6.0
prompt_toolkit>=3.0
Pygments>=2.15
pillow>=10.0
tiktoken>=0.5
numpy>=1.24
#
# Testing:
pytest>=8.0
pytest-asyncio>=0.23
pytest-timeout>=2.2
#
# Optional — uncomment as needed for specific backends/features:
# (openai is in Core above — needed at import time by memory/embeddings.py)
# flask>=3.0               # Web client server (web-client/serve.py)
# (pillow is in Core above — needed at import time by wez_math_renderer.py)
# beautifulsoup4>=4.12     # Literature / fulltext extraction
# requests>=2.31           # Literature sources, misc HTTP
# firebase-admin>=6.0      # Android push notifications
# google-api-python-client  # Gmail / Calendar integration
# google-auth-oauthlib      # Gmail / Calendar OAuth
# exa-py>=1.0              # Exa web search tool
# playwright>=1.40         # Browser automation agent
# playwright-stealth>=1.0  # Browser stealth mode
